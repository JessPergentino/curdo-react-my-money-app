//componente de conteudo do dashboard

import React from 'react'

export default props => (
    <section className='content'>{props.children}</section> //Recebe como parametro o filho - contentHeader
)